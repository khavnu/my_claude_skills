---
name: Proactively self-improve during work
description: User expects Claude to continuously find ways to become smarter and work more efficiently — update CLAUDE.md, skills, and memory as patterns are discovered
type: feedback
---

Always proactively self-improve during every conversation. Do not wait for the user to ask.

**Why:** User wants Claude to compound knowledge over time, not start fresh each session.

**How to apply:**
- When discovering a recurring pattern, convention, or pitfall in this codebase → add it to CLAUDE.md or memory immediately
- When a skill is missing or incomplete for a task encountered → write or upgrade the skill
- When a better workflow or approach is found → document it so future sessions benefit
- When debugging reveals a non-obvious root cause (Android lifecycle, Kotlin Flow edge case, etc.) → save it as a reference or feedback memory
- After solving a complex problem → reflect: "What rule would have prevented this?" → add that rule
